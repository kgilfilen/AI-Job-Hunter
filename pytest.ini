[pytest]
pythonpath = src
addopts = --import-mode=importlib
          --junitxml=test-results/pytest-results.xml
markers =
    live_ai: tests that make real OpenAI API calls and incur cost
    smoke_test_this: smoke test marker for quick test runs