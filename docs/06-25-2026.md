Parser failed to classify a posting thaat explicitly stated 'fully remote to hybrid'. This was not an LLM hallucination, it was an incomplete output schema in the prompt. Added a few other options for the work arrangement. This is why we keep evidence in the output/*.json; I went first to the job desc text to see what it found/should have done, but it kept this in the metadata.

I let copilot generate the save_fit_analysis function in parallel to the save_file for the job opening json. Getting better at trusting MS. I like the 'stem' attribute for the file name.

Accidentally overwrote fit_scorer with its own unit test...must be getting hungry. That is why we save often, and recover as needed from GIT.